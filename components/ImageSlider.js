import {View, Image} from 'react-native'

export default function ImageSliders() {
  return (
    <View>
    <Image style={styles.logo} source={require('./icons8-bank-96.png')} />
    </View>
  )
}